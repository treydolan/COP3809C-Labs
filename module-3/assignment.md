# Module 3 Interactive Lecture / Guided Lab Assignment
### Additional XML and JavaScript

Interactive meaning that you are expected to follow along with this guided lab/lecture and type/complete everything that is discussed. At the end, you will have a completed lab that will be submitted for participation points.

### XML Elements

You learned all about XML in the video in Unit 2, but here's a quick review of elements:

An XML element is everything from (including) the element's start tag to (including) the element's end tag.

Elements can even be empty:  <element></element>

### XML Naming Rules

XML elements must follow these naming rules:

Element names are case-sensitive.

Element names must start with a letter or underscore.

Element names cannot start with the letters xml (or XML, or Xml, etc).

Element names can contain letters, digits, hyphens, underscores, and periods.

Element names cannot contain spaces.

Any name can be used; no words are reserved (except xml).

### Parsing with JavaScript:

All browsers have a built-in XML parser to manipulate XML, and that can convert text into an XML DOM object.

Download and extract the stock_chart_2019 folder (from the folder called Assignment Files in the module 3 folder) and open the index.html file in notepad++. It looks like this when opened:
